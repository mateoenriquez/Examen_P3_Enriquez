#include <stdio.h>
#include <string.h>

    struct alumno
    {
        int matricula;
        char nombre [50];
        char direccion [50];
        char materia [50];
        float nota;
    };

int main() {
    
    int opcion;

    printf("---ALUMNOS UDLA---\n");
    do
    {
        printf("1. Ingresar datos de alumno\n");
        printf("2. Revisar datos de alumnos\n");
        printf("3. Salir\n");
        scanf("%d", &opcion);

        switch (opcion)
        {
        case 1:
            
            break;
        
        default:
            break;
        }

    } while (opcion!=3);

    return 0;
    
}